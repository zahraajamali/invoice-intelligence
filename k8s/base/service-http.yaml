apiVersion: v1
kind: Service
metadata:
  name: invoice-analyzer-service
  labels:
    app: invoice-analyzer
spec:
  type: ClusterIP
  selector:
    app: invoice-analyzer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
